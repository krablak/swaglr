# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users
- import: csv

transformers:

- kind: Clip
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: comment
      external_name: comment
      # Type: String Stats: 158 properties of this type in this kind.

    - property: date
      external_name: date
      # Type: Date/Time Stats: 158 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: image
      external_name: image
      # Type: Key Stats: 158 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Image')
      export_transform: transform.key_id_or_name_as_string

    - property: link
      external_name: link
      # Type: String Stats: 158 properties of this type in this kind.

    - property: page
      external_name: page
      # Type: String Stats: 158 properties of this type in this kind.

    - property: src
      external_name: src
      # Type: String Stats: 158 properties of this type in this kind.

    - property: text
      external_name: text
      # Type: String Stats: 158 properties of this type in this kind.

    - property: type
      external_name: type
      # Type: String Stats: 158 properties of this type in this kind.

    - property: user
      external_name: user
      # Type: Key Stats: 158 properties of this type in this kind.
      import_transform: transform.create_foreign_key('UserInfo')
      export_transform: transform.key_id_or_name_as_string


- kind: Image
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: tiny
      external_name: tiny
      # Type: Blob Stats: 124 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: transform.empty_if_none(base64.b64encode)

    - property: url
      external_name: url
      # Type: String Stats: 253 properties of this type in this kind.


- kind: UserInfo
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: nick
      external_name: nick
      # Type: String Stats: 5 properties of this type in this kind.

    - property: user
      external_name: user
      # Type: User Stats: 5 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: user_id
      external_name: user_id
      # Type: String Stats: 5 properties of this type in this kind.

